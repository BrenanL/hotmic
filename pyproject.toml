[project]
name = "hotmic"
version = "0.1.0"
description = "HotMic — Whisper-powered voice dictation for Windows"
requires-python = ">=3.10"
dependencies = [
    "RealtimeSTT",
    "keyboard",
    "requests",
]

[project.scripts]
hotmic = "voice_type:main"
